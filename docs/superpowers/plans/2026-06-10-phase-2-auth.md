# FlowGate Phase 2: Auth (JWT + Rollen + Guards) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
> **STATUS: BESTÄTIGT (10.06.2026)** — mit Pascal durchgegangen, Entscheidungen fixiert.

**Goal:** Login with JWT, three roles (requester/reviewer/admin), guards on both sides — backend enforces, Angular reflects. Seeded demo users, no self-registration.

**Architecture:** `users` bounded context owns the User aggregate (DDD layering, framework-free domain). `auth` context handles login/JWT and consumes the user repository **via the port exported by UsersModule** (documented exception: synchronous queries go through narrow ports; domain events are reserved for side effects — strict event-only would make a login round-trip absurd). Global `JwtAuthGuard` + `RolesGuard` with `@Public()` opt-out. Frontend: `AuthService` with signals, functional interceptor + `CanActivateFn` guards.

**Tech Stack:** @nestjs/config · @nestjs/typeorm + typeorm + pg · @nestjs/jwt · bcrypt · class-validator · Angular HttpClient/signals/Reactive Forms.

**Decisions (mit Pascal fixiert, 10.06.2026):**
1. Token-Speicherung im Frontend: **localStorage** (Demo-Trade-off bewusst akzeptiert).
2. Demo-Zugangsdaten **kommen ins README** (für lokales Ausprobieren; Deployment/Domain-Frage erst Phase 6).
3. Auth↔Users über exportierten Repository-**Port** (Events nur für Side-Effects). ✓
4. Pascal schreibt selbst: `RolesGuard.canActivate` (Task 5) + Angular-Route-Guards (Task 7).

---

### Task 1: Backend-Fundament (Config, TypeORM, Port)

**Files:**
- Modify: `backend/package.json` (deps + scripts)
- Modify: `backend/src/app.module.ts`, `backend/src/main.ts`
- Create: `backend/src/data-source.ts`
- Modify: `.env`, `.env.example` (Repo-Root)

- [ ] **Step 1: Dependencies installieren**

```bash
cd backend
npm i @nestjs/config @nestjs/typeorm typeorm pg @nestjs/jwt bcrypt class-validator class-transformer dotenv
npm i -D @types/bcrypt ts-node
```

- [ ] **Step 2: Env erweitern** — an Root-`.env` UND `.env.example` anhängen (Secret nur in `.env` echt):

```
# Backend
PORT=3001
JWT_SECRET=<openssl rand -hex 32>   # .env.example: CHANGE_ME
JWT_EXPIRES_IN=1h
```

- [ ] **Step 3: `backend/src/data-source.ts`** (TypeORM-CLI für Migrations/Seed)

```ts
import * as dotenv from 'dotenv';
import { DataSource } from 'typeorm';

dotenv.config({ path: ['../.env', '.env'] });

export default new DataSource({
  type: 'postgres',
  url: process.env.DATABASE_URL,
  entities: ['src/**/*.orm-entity.ts'],
  migrations: ['src/migrations/*.ts'],
});
```

- [ ] **Step 4: npm-Scripts ergänzen** (`backend/package.json`)

```json
"typeorm": "typeorm-ts-node-commonjs -d src/data-source.ts",
"migration:generate": "npm run typeorm -- migration:generate",
"migration:run": "npm run typeorm -- migration:run",
"seed": "ts-node src/seed.ts"
```

- [ ] **Step 5: `app.module.ts`** — Config global + TypeORM async

```ts
import { Module } from '@nestjs/common';
import { ConfigModule, ConfigService } from '@nestjs/config';
import { TypeOrmModule } from '@nestjs/typeorm';
import { AppController } from './app.controller';
import { AppService } from './app.service';

@Module({
  imports: [
    ConfigModule.forRoot({ isGlobal: true, envFilePath: ['../.env', '.env'] }),
    TypeOrmModule.forRootAsync({
      inject: [ConfigService],
      useFactory: (config: ConfigService) => ({
        type: 'postgres',
        url: config.getOrThrow<string>('DATABASE_URL'),
        autoLoadEntities: true,
        synchronize: false, // migrations only — never auto-sync
      }),
    }),
  ],
  controllers: [AppController],
  providers: [AppService],
})
export class AppModule {}
```

- [ ] **Step 6: `main.ts`** — Prefix, Validation, CORS, Port 3001 (3000 ist auf Pascals Maschine belegt)

```ts
import { ValidationPipe } from '@nestjs/common';
import { NestFactory } from '@nestjs/core';
import { AppModule } from './app.module';

async function bootstrap() {
  const app = await NestFactory.create(AppModule);
  app.setGlobalPrefix('api/v1');
  app.useGlobalPipes(new ValidationPipe({ whitelist: true, transform: true }));
  app.enableCors({ origin: ['http://localhost:4200'] });
  await app.listen(process.env.PORT ?? 3001);
}
bootstrap().catch((err) => {
  console.error('Fatal error during bootstrap', err);
  process.exit(1);
});
```

- [ ] **Step 7: Verifizieren + Commit**

Run: `npx tsc --noEmit && npm run lint && PORT=3001 npm run start` → bootet, verbindet zu Postgres (Log: `TypeOrmModule dependencies initialized`). Stoppen.
```bash
git add -A && git commit -m "feat(backend): add config, typeorm wiring and api prefix"
```

### Task 2: `users`-Context — Domain + Persistence + Migration

**Files:**
- Create: `backend/src/users/domain/value-objects/role.ts`
- Create: `backend/src/users/domain/entities/user.ts`
- Create: `backend/src/users/domain/repositories/user.repository.ts`
- Create: `backend/src/users/infrastructure/persistence/user.orm-entity.ts`
- Create: `backend/src/users/infrastructure/repositories/typeorm-user.repository.ts`
- Create: `backend/src/users/users.module.ts`
- Create: `backend/src/migrations/<ts>-CreateUsers.ts` (generiert)

- [ ] **Step 1: Domain (framework-frei — kein einziger @nestjs/TypeORM-Import!)**

`role.ts`:
```ts
export enum Role {
  Requester = 'requester',
  Reviewer = 'reviewer',
  Admin = 'admin',
}
```

`user.ts`:
```ts
import { Role } from '../value-objects/role';

export class User {
  constructor(
    public readonly id: string,
    public readonly email: string,
    public readonly passwordHash: string,
    public readonly role: Role,
    public readonly createdAt: Date,
  ) {}
}
```

`user.repository.ts`:
```ts
import { User } from '../entities/user';

export const USER_REPOSITORY = Symbol('USER_REPOSITORY');

export interface UserRepository {
  findByEmail(email: string): Promise<User | null>;
  findById(id: string): Promise<User | null>;
  findAll(): Promise<User[]>;
}
```

- [ ] **Step 2: Infrastructure**

`user.orm-entity.ts`:
```ts
import { Column, CreateDateColumn, Entity, PrimaryGeneratedColumn } from 'typeorm';
import { Role } from '../../domain/value-objects/role';

@Entity('users')
export class UserOrmEntity {
  @PrimaryGeneratedColumn('uuid')
  id!: string;

  @Column({ unique: true })
  email!: string;

  @Column({ name: 'password_hash' })
  passwordHash!: string;

  @Column({ type: 'enum', enum: Role, enumName: 'user_role' })
  role!: Role;

  @CreateDateColumn({ name: 'created_at' })
  createdAt!: Date;
}
```

`typeorm-user.repository.ts`:
```ts
import { Injectable } from '@nestjs/common';
import { InjectRepository } from '@nestjs/typeorm';
import { Repository } from 'typeorm';
import { User } from '../../domain/entities/user';
import { UserRepository } from '../../domain/repositories/user.repository';
import { UserOrmEntity } from '../persistence/user.orm-entity';

@Injectable()
export class TypeormUserRepository implements UserRepository {
  constructor(
    @InjectRepository(UserOrmEntity)
    private readonly repo: Repository<UserOrmEntity>,
  ) {}

  async findByEmail(email: string): Promise<User | null> {
    return this.toDomain(await this.repo.findOneBy({ email }));
  }

  async findById(id: string): Promise<User | null> {
    return this.toDomain(await this.repo.findOneBy({ id }));
  }

  async findAll(): Promise<User[]> {
    const rows = await this.repo.find({ order: { createdAt: 'ASC' } });
    return rows.map((r) => this.toDomain(r)!);
  }

  private toDomain(row: UserOrmEntity | null): User | null {
    if (!row) return null;
    return new User(row.id, row.email, row.passwordHash, row.role, row.createdAt);
  }
}
```

`users.module.ts` (exportiert den **Port**, nicht die Implementierung):
```ts
import { Module } from '@nestjs/common';
import { TypeOrmModule } from '@nestjs/typeorm';
import { USER_REPOSITORY } from './domain/repositories/user.repository';
import { UserOrmEntity } from './infrastructure/persistence/user.orm-entity';
import { TypeormUserRepository } from './infrastructure/repositories/typeorm-user.repository';

@Module({
  imports: [TypeOrmModule.forFeature([UserOrmEntity])],
  providers: [{ provide: USER_REPOSITORY, useClass: TypeormUserRepository }],
  exports: [USER_REPOSITORY],
})
export class UsersModule {}
```
In `app.module.ts` importieren.

- [ ] **Step 3: Migration generieren + ausführen**

```bash
npm run migration:generate src/migrations/CreateUsers
npm run migration:run
```
Expected: Tabelle `users` + Enum `user_role` in Postgres (`\dt` zeigt `users`, `migrations`).

- [ ] **Step 4: Gates + Commit**

`npx tsc --noEmit && npm run lint` → grün.
```bash
git add -A && git commit -m "feat(users): add user domain, typeorm persistence and migration"
```

### Task 3: Seed-Script (3 Demo-User)

**Files:**
- Create: `backend/src/seed.ts`

- [ ] **Step 1: `seed.ts`** (idempotent — überspringt existierende)

```ts
import * as bcrypt from 'bcrypt';
import dataSource from './data-source';
import { Role } from './users/domain/value-objects/role';
import { UserOrmEntity } from './users/infrastructure/persistence/user.orm-entity';

const DEMO_USERS: Array<{ email: string; role: Role }> = [
  { email: 'admin@flowgate.demo', role: Role.Admin },
  { email: 'reviewer@flowgate.demo', role: Role.Reviewer },
  { email: 'requester@flowgate.demo', role: Role.Requester },
];

async function seed(): Promise<void> {
  await dataSource.initialize();
  const repo = dataSource.getRepository(UserOrmEntity);
  const password = process.env.SEED_PASSWORD ?? 'Demo1234!';
  const passwordHash = await bcrypt.hash(password, 10);

  for (const { email, role } of DEMO_USERS) {
    if (await repo.existsBy({ email })) {
      console.log(`skip (exists): ${email}`);
      continue;
    }
    await repo.save(repo.create({ email, passwordHash, role }));
    console.log(`created: ${email} (${role})`);
  }
  await dataSource.destroy();
}

seed().catch((err) => {
  console.error(err);
  process.exit(1);
});
```

- [ ] **Step 2: Ausführen + verifizieren**

Run: `npm run seed` → 3× `created`. Nochmal → 3× `skip`.
Run: `docker compose exec postgres psql -U flowgate -d flowgate -c 'select email, role from users;'` → 3 Zeilen.

- [ ] **Step 3: Gates + Commit** (`tsc`/Lint, dann)

```bash
git add -A && git commit -m "feat(users): add idempotent demo user seed script"
```

### Task 4: `auth`-Context — Login (TDD)

**Files:**
- Create: `backend/src/auth/application/dto/login.dto.ts`
- Create: `backend/src/auth/application/login.use-case.ts`
- Test: `backend/src/auth/application/login.use-case.spec.ts`
- Create: `backend/src/auth/auth.controller.ts`, `backend/src/auth/auth.module.ts`
- Create: `backend/src/shared/types/auth-user.ts`

- [ ] **Step 1: Shared Payload-Typ** (`auth-user.ts`)

```ts
import { Role } from '../../users/domain/value-objects/role';

export interface AuthUser {
  sub: string; // user id
  email: string;
  role: Role;
}
```

- [ ] **Step 2: DTO** (`login.dto.ts`)

```ts
import { IsEmail, IsString, MinLength } from 'class-validator';

export class LoginDto {
  @IsEmail()
  email!: string;

  @IsString()
  @MinLength(8)
  password!: string;
}
```

- [ ] **Step 3: FAILING TEST schreiben** (`login.use-case.spec.ts`)

```ts
import { UnauthorizedException } from '@nestjs/common';
import { JwtService } from '@nestjs/jwt';
import * as bcrypt from 'bcrypt';
import { User } from '../../users/domain/entities/user';
import { Role } from '../../users/domain/value-objects/role';
import { LoginUseCase } from './login.use-case';

describe('LoginUseCase', () => {
  const jwt = { signAsync: jest.fn().mockResolvedValue('signed.jwt') } as unknown as JwtService;
  let user: User;

  beforeAll(async () => {
    user = new User('u1', 'admin@flowgate.demo', await bcrypt.hash('Demo1234!', 4), Role.Admin, new Date());
  });

  const repoWith = (found: User | null) => ({
    findByEmail: jest.fn().mockResolvedValue(found),
    findById: jest.fn(),
    findAll: jest.fn(),
  });

  it('returns an access token for valid credentials', async () => {
    const useCase = new LoginUseCase(repoWith(user), jwt);
    const result = await useCase.execute({ email: 'admin@flowgate.demo', password: 'Demo1234!' });
    expect(result).toEqual({ accessToken: 'signed.jwt' });
  });

  it('rejects a wrong password', async () => {
    const useCase = new LoginUseCase(repoWith(user), jwt);
    await expect(useCase.execute({ email: 'admin@flowgate.demo', password: 'wrong-pass' })).rejects.toBeInstanceOf(UnauthorizedException);
  });

  it('rejects an unknown email', async () => {
    const useCase = new LoginUseCase(repoWith(null), jwt);
    await expect(useCase.execute({ email: 'ghost@flowgate.demo', password: 'Demo1234!' })).rejects.toBeInstanceOf(UnauthorizedException);
  });
});
```

Run: `npm test -- login.use-case` → FAIL (LoginUseCase existiert nicht).

- [ ] **Step 4: Use-Case implementieren** (`login.use-case.ts`)

```ts
import { Inject, Injectable, UnauthorizedException } from '@nestjs/common';
import { JwtService } from '@nestjs/jwt';
import * as bcrypt from 'bcrypt';
import { AuthUser } from '../../shared/types/auth-user';
import { USER_REPOSITORY, UserRepository } from '../../users/domain/repositories/user.repository';
import { LoginDto } from './dto/login.dto';

@Injectable()
export class LoginUseCase {
  constructor(
    @Inject(USER_REPOSITORY) private readonly users: UserRepository,
    private readonly jwt: JwtService,
  ) {}

  async execute(dto: LoginDto): Promise<{ accessToken: string }> {
    const user = await this.users.findByEmail(dto.email.toLowerCase());
    const passwordOk = user !== null && (await bcrypt.compare(dto.password, user.passwordHash));
    if (!user || !passwordOk) {
      // same error for both cases — don't leak which emails exist
      throw new UnauthorizedException('Invalid credentials');
    }
    const payload: AuthUser = { sub: user.id, email: user.email, role: user.role };
    return { accessToken: await this.jwt.signAsync(payload) };
  }
}
```

Run: `npm test -- login.use-case` → PASS (3/3).

- [ ] **Step 5: Controller + Module**

`auth.controller.ts`:
```ts
import { Body, Controller, HttpCode, Post } from '@nestjs/common';
import { Public } from '../shared/decorators/public.decorator';
import { LoginDto } from './application/dto/login.dto';
import { LoginUseCase } from './application/login.use-case';

@Controller('auth')
export class AuthController {
  constructor(private readonly loginUseCase: LoginUseCase) {}

  @Public()
  @Post('login')
  @HttpCode(200)
  login(@Body() dto: LoginDto): Promise<{ accessToken: string }> {
    return this.loginUseCase.execute(dto);
  }
}
```

`auth.module.ts`:
```ts
import { Module } from '@nestjs/common';
import { ConfigService } from '@nestjs/config';
import { JwtModule } from '@nestjs/jwt';
import { UsersModule } from '../users/users.module';
import { LoginUseCase } from './application/login.use-case';
import { AuthController } from './auth.controller';

@Module({
  imports: [
    UsersModule,
    JwtModule.registerAsync({
      global: true,
      inject: [ConfigService],
      useFactory: (config: ConfigService) => ({
        secret: config.getOrThrow<string>('JWT_SECRET'),
        signOptions: { expiresIn: config.get('JWT_EXPIRES_IN', '1h') },
      }),
    }),
  ],
  controllers: [AuthController],
  providers: [LoginUseCase],
})
export class AuthModule {}
```
In `app.module.ts` importieren. (Hinweis: `@Public()` entsteht in Task 5, Step 1 — Task 5 vor dem Boot-Test ausführen.)

- [ ] **Step 6: Gates + Commit**

`npx tsc --noEmit && npm run lint && npm test` → grün.
```bash
git add -A && git commit -m "feat(auth): add jwt login use case and endpoint (tdd)"
```

### Task 5: Guards + RBAC + geschützte Endpoints

**Files:**
- Create: `backend/src/shared/decorators/public.decorator.ts`, `roles.decorator.ts`, `current-user.decorator.ts`
- Create: `backend/src/shared/guards/jwt-auth.guard.ts`, `roles.guard.ts`
- Create: `backend/src/users/application/users.controller.ts` (admin-only Liste)
- Modify: `backend/src/auth/auth.controller.ts` (+ `GET /auth/me`), `app.module.ts` (globale Guards)
- Test: `backend/test/auth.e2e-spec.ts`

- [ ] **Step 1: Decorators**

`public.decorator.ts`:
```ts
import { SetMetadata } from '@nestjs/common';
export const IS_PUBLIC_KEY = 'isPublic';
export const Public = () => SetMetadata(IS_PUBLIC_KEY, true);
```

`roles.decorator.ts`:
```ts
import { SetMetadata } from '@nestjs/common';
import { Role } from '../../users/domain/value-objects/role';
export const ROLES_KEY = 'roles';
export const Roles = (...roles: Role[]) => SetMetadata(ROLES_KEY, roles);
```

`current-user.decorator.ts`:
```ts
import { createParamDecorator, ExecutionContext } from '@nestjs/common';
import { AuthUser } from '../types/auth-user';

export const CurrentUser = createParamDecorator((_: unknown, ctx: ExecutionContext): AuthUser => {
  const req = ctx.switchToHttp().getRequest<{ user: AuthUser }>();
  return req.user;
});
```

- [ ] **Step 2: `jwt-auth.guard.ts`** (global, `@Public()` umgeht ihn)

```ts
import { CanActivate, ExecutionContext, Injectable, UnauthorizedException } from '@nestjs/common';
import { Reflector } from '@nestjs/core';
import { JwtService } from '@nestjs/jwt';
import type { Request } from 'express';
import { IS_PUBLIC_KEY } from '../decorators/public.decorator';
import { AuthUser } from '../types/auth-user';

@Injectable()
export class JwtAuthGuard implements CanActivate {
  constructor(
    private readonly jwt: JwtService,
    private readonly reflector: Reflector,
  ) {}

  async canActivate(ctx: ExecutionContext): Promise<boolean> {
    const isPublic = this.reflector.getAllAndOverride<boolean>(IS_PUBLIC_KEY, [ctx.getHandler(), ctx.getClass()]);
    if (isPublic) return true;

    const req = ctx.switchToHttp().getRequest<Request & { user?: AuthUser }>();
    const [scheme, token] = req.headers.authorization?.split(' ') ?? [];
    if (scheme !== 'Bearer' || !token) throw new UnauthorizedException();
    try {
      req.user = await this.jwt.verifyAsync<AuthUser>(token);
      return true;
    } catch {
      throw new UnauthorizedException();
    }
  }
}
```

- [ ] **Step 3: `roles.guard.ts`** — **→ PASCAL-BEITRAG:** den `canActivate`-Body (~8 Zeilen) schreibt Pascal (Lernziel: Reflector + Metadata verstehen). Gerüst:

```ts
import { CanActivate, ExecutionContext, Injectable } from '@nestjs/common';
import { Reflector } from '@nestjs/core';
import { ROLES_KEY } from '../decorators/roles.decorator';
import { AuthUser } from '../types/auth-user';
import { Role } from '../../users/domain/value-objects/role';

@Injectable()
export class RolesGuard implements CanActivate {
  constructor(private readonly reflector: Reflector) {}

  canActivate(ctx: ExecutionContext): boolean {
    // TODO(Pascal): 1) benötigte Rollen via reflector.getAllAndOverride<Role[]>(ROLES_KEY, …) lesen
    //               2) keine Rollen gefordert → true
    //               3) sonst: req.user.role gegen geforderte Rollen prüfen
    return true;
  }
}
```

- [ ] **Step 4: Global registrieren** (`app.module.ts` providers — Reihenfolge: erst Auth, dann Roles)

```ts
import { APP_GUARD } from '@nestjs/core';
import { JwtAuthGuard } from './shared/guards/jwt-auth.guard';
import { RolesGuard } from './shared/guards/roles.guard';
// in providers:
{ provide: APP_GUARD, useClass: JwtAuthGuard },
{ provide: APP_GUARD, useClass: RolesGuard },
```

- [ ] **Step 5: Endpoints** — `GET /auth/me` (jeder eingeloggte User) + `GET /users` (admin-only):

`auth.controller.ts` ergänzen:
```ts
@Get('me')
me(@CurrentUser() user: AuthUser): AuthUser {
  return user;
}
```

`users/application/users.controller.ts`:
```ts
import { Controller, Get, Inject } from '@nestjs/common';
import { Roles } from '../../shared/decorators/roles.decorator';
import { Role } from '../domain/value-objects/role';
import { USER_REPOSITORY, UserRepository } from '../domain/repositories/user.repository';

@Controller('users')
export class UsersController {
  constructor(@Inject(USER_REPOSITORY) private readonly users: UserRepository) {}

  @Get()
  @Roles(Role.Admin)
  async findAll(): Promise<Array<{ id: string; email: string; role: Role }>> {
    const all = await this.users.findAll();
    return all.map(({ id, email, role }) => ({ id, email, role })); // never expose passwordHash
  }
}
```
In `users.module.ts` unter `controllers` eintragen.

- [ ] **Step 6: e2e-Test** (`test/auth.e2e-spec.ts`) — Flow gegen laufende DB:

```ts
import { INestApplication, ValidationPipe } from '@nestjs/common';
import { Test } from '@nestjs/testing';
import * as request from 'supertest';
import { AppModule } from '../src/app.module';

describe('Auth (e2e)', () => {
  let app: INestApplication;
  let token: string;

  beforeAll(async () => {
    const moduleRef = await Test.createTestingModule({ imports: [AppModule] }).compile();
    app = moduleRef.createNestApplication();
    app.setGlobalPrefix('api/v1');
    app.useGlobalPipes(new ValidationPipe({ whitelist: true, transform: true }));
    await app.init();
  });

  afterAll(async () => app.close());

  it('POST /auth/login → 200 + token', async () => {
    const res = await request(app.getHttpServer())
      .post('/api/v1/auth/login')
      .send({ email: 'requester@flowgate.demo', password: 'Demo1234!' })
      .expect(200);
    token = res.body.accessToken;
    expect(token).toBeDefined();
  });

  it('GET /auth/me without token → 401', () =>
    request(app.getHttpServer()).get('/api/v1/auth/me').expect(401));

  it('GET /auth/me with token → 200 + role', async () => {
    const res = await request(app.getHttpServer())
      .get('/api/v1/auth/me')
      .set('Authorization', `Bearer ${token}`)
      .expect(200);
    expect(res.body.role).toBe('requester');
  });

  it('GET /users as requester → 403 (admin only)', () =>
    request(app.getHttpServer()).get('/api/v1/users').set('Authorization', `Bearer ${token}`).expect(403));
});
```

Run: `npm run test:e2e` → 4/4 PASS (setzt laufenden Postgres + Seed voraus).

- [ ] **Step 7: Gates + Commit**

```bash
git add -A && git commit -m "feat(auth): add global jwt/roles guards, me endpoint and admin user list"
```

### Task 6: Frontend — AuthService, Interceptor, Environments

**Files:**
- Create: `frontend/src/environments/` (via CLI)
- Create: `frontend/src/app/core/auth/current-user.ts`, `auth.service.ts`, `auth.interceptor.ts`
- Modify: `frontend/src/app/app.config.ts`

- [ ] **Step 1: Environments generieren**

```bash
cd frontend && npx ng generate environments
```
`environment.development.ts`: `apiUrl: 'http://localhost:3001/api/v1'` · `environment.ts` (prod): `apiUrl: '/api/v1'`.

```ts
export const environment = {
  apiUrl: 'http://localhost:3001/api/v1',
};
```

- [ ] **Step 2: `current-user.ts`**

```ts
export type Role = 'requester' | 'reviewer' | 'admin';

export interface CurrentUser {
  sub: string;
  email: string;
  role: Role;
  exp: number; // unix seconds
}
```

- [ ] **Step 3: `auth.service.ts`** (Signals; Token-Speicherung = Entscheidung 1)

```ts
import { HttpClient } from '@angular/common/http';
import { Injectable, computed, inject, signal } from '@angular/core';
import { Observable, tap } from 'rxjs';
import { environment } from '../../../environments/environment';
import { CurrentUser } from './current-user';

const TOKEN_KEY = 'flowgate.token';

@Injectable({ providedIn: 'root' })
export class AuthService {
  private readonly http = inject(HttpClient);
  private readonly token = signal<string | null>(localStorage.getItem(TOKEN_KEY));

  readonly currentUser = computed<CurrentUser | null>(() => this.decode(this.token()));
  readonly isLoggedIn = computed(() => this.currentUser() !== null);

  login(email: string, password: string): Observable<{ accessToken: string }> {
    return this.http
      .post<{ accessToken: string }>(`${environment.apiUrl}/auth/login`, { email, password })
      .pipe(tap(({ accessToken }) => this.setToken(accessToken)));
  }

  logout(): void {
    this.setToken(null);
  }

  rawToken(): string | null {
    return this.token();
  }

  private setToken(value: string | null): void {
    value === null ? localStorage.removeItem(TOKEN_KEY) : localStorage.setItem(TOKEN_KEY, value);
    this.token.set(value);
  }

  private decode(token: string | null): CurrentUser | null {
    if (!token) return null;
    try {
      const payload = JSON.parse(atob(token.split('.')[1])) as CurrentUser;
      return payload.exp * 1000 > Date.now() ? payload : null; // expired → treat as logged out
    } catch {
      return null;
    }
  }
}
```

- [ ] **Step 4: `auth.interceptor.ts`** (functional)

```ts
import { HttpInterceptorFn } from '@angular/common/http';
import { inject } from '@angular/core';
import { AuthService } from './auth.service';

export const authInterceptor: HttpInterceptorFn = (req, next) => {
  const token = inject(AuthService).rawToken();
  return next(token ? req.clone({ setHeaders: { Authorization: `Bearer ${token}` } }) : req);
};
```

- [ ] **Step 5: `app.config.ts`** — HttpClient + Interceptor registrieren

```ts
import { provideHttpClient, withInterceptors } from '@angular/common/http';
import { authInterceptor } from './core/auth/auth.interceptor';
// in providers:
provideHttpClient(withInterceptors([authInterceptor])),
```

- [ ] **Step 6: Gates + Commit**

`npx ng build && npx ng lint` → grün.
```bash
git add -A && git commit -m "feat(frontend): add auth service with signals and jwt interceptor"
```

### Task 7: Frontend — Login-Seite, Route-Guards, Routen

**Files:**
- Create: `frontend/src/app/features/login/` (`ng g c features/login`)
- Create: `frontend/src/app/features/dashboard/` (Platzhalter, `ng g c features/dashboard`)
- Create: `frontend/src/app/core/auth/auth.guard.ts`
- Modify: `frontend/src/app/app.routes.ts`

- [ ] **Step 1: `auth.guard.ts`** — **→ PASCAL-BEITRAG:** beide Guards (~10 Zeilen) schreibt Pascal (Lernziel: CanActivateFn + inject + UrlTree-Redirect). Gerüst:

```ts
import { CanActivateFn } from '@angular/router';

// TODO(Pascal): authGuard — wenn AuthService.isLoggedIn() → true, sonst UrlTree nach /login
export const authGuard: CanActivateFn = () => true;

// TODO(Pascal): roleGuard(...roles) — Factory, die eine CanActivateFn zurückgibt:
// eingeloggt UND Rolle enthalten → true, sonst UrlTree nach /
export const roleGuard = (..._roles: string[]): CanActivateFn => () => true;
```

- [ ] **Step 2: Login-Komponente** (`login.ts` — Reactive Form, nonNullable)

```ts
import { Component, inject, signal } from '@angular/core';
import { NonNullableFormBuilder, ReactiveFormsModule, Validators } from '@angular/forms';
import { Router } from '@angular/router';
import { AuthService } from '../../core/auth/auth.service';

@Component({
  selector: 'app-login',
  imports: [ReactiveFormsModule],
  templateUrl: './login.html',
  styleUrl: './login.scss',
})
export class Login {
  private readonly fb = inject(NonNullableFormBuilder);
  private readonly auth = inject(AuthService);
  private readonly router = inject(Router);

  readonly error = signal<string | null>(null);
  readonly form = this.fb.group({
    email: ['', [Validators.required, Validators.email]],
    password: ['', [Validators.required, Validators.minLength(8)]],
  });

  submit(): void {
    if (this.form.invalid) {
      this.form.markAllAsTouched();
      return;
    }
    const { email, password } = this.form.getRawValue();
    this.auth.login(email, password).subscribe({
      next: () => void this.router.navigateByUrl('/'),
      error: () => this.error.set('E-Mail oder Passwort ist falsch.'),
    });
  }
}
```

`login.html`:
```html
<form [formGroup]="form" (ngSubmit)="submit()">
  <h1>FlowGate Login</h1>
  <label>E-Mail <input type="email" formControlName="email" /></label>
  <label>Passwort <input type="password" formControlName="password" /></label>
  @if (error(); as msg) {
    <p class="error" role="alert">{{ msg }}</p>
  }
  <button type="submit" [disabled]="form.invalid">Anmelden</button>
</form>
```

- [ ] **Step 3: Routen** (`app.routes.ts`)

```ts
import { Routes } from '@angular/router';
import { authGuard } from './core/auth/auth.guard';

export const routes: Routes = [
  { path: 'login', loadComponent: () => import('./features/login/login').then((m) => m.Login) },
  {
    path: '',
    canActivate: [authGuard],
    loadComponent: () => import('./features/dashboard/dashboard').then((m) => m.Dashboard),
  },
  { path: '**', redirectTo: '' },
];
```

Dashboard-Platzhalter zeigt `auth.currentUser()` (E-Mail + Rolle) + Logout-Button.

- [ ] **Step 4: Manueller Smoke-Test (beide Server laufen)**

Backend: `cd backend && npm run start:dev` · Frontend: `cd frontend && npx ng serve`.
Browser `http://localhost:4200` → Redirect auf `/login` → Login `requester@flowgate.demo` / `Demo1234!` → Dashboard zeigt E-Mail+Rolle → Reload → eingeloggt bleibt (localStorage) → Logout → wieder `/login`.

- [ ] **Step 5: Gates + Commit + Push**

`ng build`, `ng lint`, backend `tsc`/`lint`/`test` → alles grün.
```bash
git add -A && git commit -m "feat(frontend): add login page, route guards and protected dashboard"
git push
```

### Task 8: README-Update

- [ ] README: Abschnitt „Getting started" (compose up, migration:run, seed, beide Dev-Server) + Demo-Logins (falls Entscheidung 2 = ja).
- [ ] Commit: `docs: add getting started and demo credentials` + Push.

---

## Definition of Done (Phase 2)
- Login per JWT funktioniert end-to-end (Browser-Flow wie Task 7 Step 4).
- Backend: Unit-Tests (LoginUseCase 3/3) + e2e (4/4) grün; `tsc` + Lint grün.
- Frontend: Build + Lint grün; Route-Guards verhindern Zugriff ohne Login.
- RBAC nachweisbar: `GET /users` nur als Admin (403 sonst).
- Keine Secrets im Repo (`JWT_SECRET` nur in lokaler `.env`).
